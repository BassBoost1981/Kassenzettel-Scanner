{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:path:default",
    "shell:allow-spawn",
    "shell:allow-execute",
    "dialog:default",
    "fs:default"
  ]
}
